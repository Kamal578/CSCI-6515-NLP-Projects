# NLP Project — Azerbaijani Wikipedia Corpus

## Quickstart
1) Create venv + install deps:
   python -m venv .venv
   source .venv/bin/activate
   pip install -r requirements.txt

2) Download corpus:
   python -m src.pull_wikipedia --help

3) Run pipeline:
   python -m src.main
